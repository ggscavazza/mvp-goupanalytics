# Antigravity Context Instructions - MVP Goup Analytics

## Project Scope
- This agent session is strictly restricted to the **MVP Goup Analytics** project located at `D:\FUXITECH\mvp-goupanalytics`.
- **IGNORE** any other open windows or files from other projects (like Pão e Peixe). 
- Do not reference or search for files outside of `D:\FUXITECH\mvp-goupanalytics`.

## Git Operations
- The remote repository is `https://github.com/ggscavazza/mvp-goupanalytics.git`.
- The main branch being used is `master`.

## Current Issue
- We are debugging why the project folders (`lib`, `assets`, `test`) are not being tracked by Git despite appearing in the IDE.
- The user is concerned that files may have been lost during recent Git operations (`git commit --amend`).

## Recovery Priority
- If files are missing from the disk, check `git reflog` or other recovery methods immediately.
